# --- Database ---
POSTGRES_USER=job_platform
POSTGRES_PASSWORD=changeme_local_only
POSTGRES_DB=job_platform
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Full connection string used by SQLAlchemy/Alembic
DATABASE_URL=postgresql+psycopg2://job_platform:changeme_local_only@db:5432/job_platform

# --- App ---
APP_ENV=local
LOG_LEVEL=INFO

# --- Future phases (left blank on purpose, not required yet) ---
# LLM_API_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=
